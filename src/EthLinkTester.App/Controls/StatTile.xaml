<UserControl
    x:Class="EthLinkTester.App.Controls.StatTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="14,10"
            MinWidth="108">
        <StackPanel Spacing="2">

            <TextBlock Text="{x:Bind Label, Mode=OneWay}"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                <!--
                  Value and unit both wear text tokens rather than a series colour. A coloured
                  mark beside a number carries identity; colouring the number itself does not.
                -->
                <TextBlock Text="{x:Bind Value, Mode=OneWay}"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                <TextBlock Text="{x:Bind Unit, Mode=OneWay}"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                           Margin="0,0,0,4"
                           VerticalAlignment="Bottom" />
            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>
